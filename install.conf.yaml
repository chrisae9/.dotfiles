- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
        force: true
    ~/.vimrc:
    ~/.gitconfig:
        force: true

- shell:
  - [git submodule update --init --recursive, Installing submodules]
