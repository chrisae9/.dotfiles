- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
        force: true
    ~/.vimrc:
        relink: true
    ~/.gitconfig:
        relink: true
        force: true

- shell:
  - [git submodule update --init --recursive, Installing submodules]
